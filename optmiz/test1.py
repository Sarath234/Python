import numpy as np
#a=[1,2.3,5,9,7,3,6,3,.5]
#print np.argmin(a)
vms=[0]*10
print vms.count(0)
#print sum(e.count(0) for e in vms)
'''vms[9]=3
vms[3]=5

def vmupdation():
    totalvms=10
    busyvms=0
    for n in range(0,len(vms)):
        if vms[n]!=0:
            busyvms+=1
    av_vms=totalvms-busyvms
    return av_vms

def vm(vms):
    argmt=[3,2]
    minv=[8,2]
    vm=[]
    vms=sorted(vms)
    print vms
    for i in range(0,len(argmt)):
        for j in range(0,argmt[i]):
            vm.append(minv[i])
    for i in range(0,len(vm)):
        vms[i]=vm[i]
    return vms

print vm(vms)'''
